%
% tete_de_veau_ravigote_notes.tex
% Notes seules (notes finales), sans le contenu du roman.
%
% Généré par : build_all.sh notes
%   1. pdflatex "\def\AVECNOTES{}\input{tete_de_veau_ravigote}"  (×2 → .ent)
%   2. latexmk -pdf tete_de_veau_ravigote-notes.tex  → tete_de_veau_ravigote-notes.pdf
%
\documentclass[11pt,a5paper]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{ebgaramond}
\usepackage[french]{babel}
\usepackage{microtype}
\usepackage{endnotes}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{tikz}
\usepackage{catchfile}
\usepackage[left=22mm,right=17mm,top=22mm,bottom=25mm]{geometry}
\input{version.tex}
\CatchFileDef{\gitcommit}{tete_de_veau_ravigote.gitinfo}{\endlinechar=-1}

\setlength{\parindent}{0pt}
\setlength{\parskip}{4pt}

% ─── Macros présentes dans le .ent ────────────────────────────────────────────

% Titre de section de notes (acte N)
\newcommand{\chapnoteheader}[1]{{\normalfont\scshape #1}}

% Source Wikipedia en fin de note
\newcommand{\source}[1]{\newline\textit{Source~:} \textup{#1}}
% Image seule dans une note
\newcommand{\nfimg}[2]{\newline\includegraphics[width=#1\linewidth]{#2}}
% Image côte à côte avec le texte
\newcommand{\nfimgblock}[4]{%
  \begin{minipage}[t]{0.45\linewidth}\small#1\end{minipage}%
  \hfill%
  \begin{minipage}[t]{#2\linewidth}\includegraphics[width=\linewidth]{#3}\end{minipage}%
  \source{#4}}

% ─── Format des notes : identique au roman ────────────────────────────────────
\renewcommand{\notesname}{}
\renewcommand{\enotesize}{\small}
\makeatletter
\renewcommand\enoteformat{%
  \rightskip\z@ \leftskip\z@ \parindent=0pt
  \leavevmode\theenmark.\enspace}
\makeatother

% ─── Document ─────────────────────────────────────────────────────────────────
\begin{document}
\pagestyle{plain}

% ═══ PAGE DE TITRE ════════════════════════════════════════════════════════════
\thispagestyle{empty}
\begin{center}
\vspace*{48pt}
{\scshape\large éric mugnier}

\vspace{64pt}
{\scshape\huge tête de veau ravigote}

\vspace{96pt}
{\itshape notes}
\end{center}

% Avancer à la première page impaire après le titre
\newpage
\makeatletter
\ifodd\c@page\else
  \hbox{}\thispagestyle{empty}\newpage
\fi
\makeatother

% ═══ NOTES ════════════════════════════════════════════════════════════════════
% \theendnotes lit \jobname.ent — on masque \jobname pour pointer vers le .ent du roman
\begingroup
  \def\jobname{tete_de_veau_ravigote}%
  \theendnotes
\endgroup

% ═══ COLOPHON ═════════════════════════════════════════════════════════════════
\input{colophon}

\end{document}
