\documentclass[tikz, border=0pt]{standalone}

% ─── PACKAGES ─────────────────────────────────────────────────────────────────
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{ebgaramond}
\usepackage[french]{babel}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{calc}
\usepackage{microtype}
\usepackage{graphicx}

% ─── COULEUR ──────────────────────────────────────────────────────────────────
\definecolor{encre}{RGB}{18,18,18}
\definecolor{gristexte}{RGB}{90,90,90}
\definecolor{bleutnuit}{RGB}{28,40,64}

% ─── DOCUMENT ─────────────────────────────────────────────────────────────────
\begin{document}

% ─── CANVAS TIKZ (unité = 1mm, origine bas-gauche) ───────────────────────────
%
% Gabarit 24Bookprint — format 155×235 mm — ~530 pages
%
%   Fond perdu          : 3 mm de chaque côté
%   Marge de sécurité   : ~14 mm en retrait des traits de coupe (texte/image)
%
%   Zones (x) :
%     Fond perdu gauche   :   0 –   3 mm
%     4e de couverture    :   3 – 158 mm  (155 mm)
%     Dos                 : 158 – 207,5 mm  (49,5 mm)
%     1ère de couverture  : 207,5 – 362,5 mm  (155 mm)
%     Fond perdu droit    : 362,5 – 365,5 mm
%
%   Zones (y) :
%     Fond perdu bas      :   0 –   3 mm
%     Contenu             :   3 – 238 mm  (235 mm)
%     Fond perdu haut     : 238 – 241 mm
%
%   Centres :
%     4e de couverture    :  80,5 mm
%     Dos                 : 182,75 mm
%     1ère de couverture  : 285 mm

\begin{tikzpicture}[x=1mm, y=1mm, font=\normalfont]

  % ── FOND BLANC ABSOLU ───────────────────────────────────────────────────────
  \fill[white] (0,0) rectangle (365.5,241);

  % ═══════════════════════════════════════════════════════════════════════════
  % ══ 1ÈRE DE COUVERTURE (207,5 – 362,5 mm) — centre : 285 mm ══════════════
  % ═══════════════════════════════════════════════════════════════════════════

  % ── Auteur : petites caps, très espacées, centré entre haut et filet ────────
  \node[anchor=center, text=bleutnuit] at (285, 203)
    {\fontsize{20}{24}\selectfont\textls[250]{\textsc{éric mugnier}}};

  % ── Filet unique : l'unique élément graphique ─────────────────────────────
  \draw[bleutnuit, line width=0.25pt] (219.5, 175) -- (350.5, 175);

  % ── Titre : garamond regular, grand, aéré, une seule ligne ────────────────
  \node[
    anchor=north,
    align=center,
    text=bleutnuit
  ] at (285, 171)
  {\fontsize{30}{36}\selectfont
    Tête de veau ravigote};

  % ── Logo CT (1ère, bas droite, discret) ──────────────────────────────────
  \node[anchor=south, opacity=1] at (285, 10)
    {\includegraphics[width=6mm]{images/ct_watermark.png}};

  % ═══════════════════════════════════════════════════════════════════════════
  % ══ DOS (158 – 207,5 mm) — centre : 182,75 mm ════════════════════════════
  % ═══════════════════════════════════════════════════════════════════════════

  % Filets de séparation, très effacés
  \draw[encre!12, line width=0.2pt] (158,0) -- (158,241);
  \draw[encre!12, line width=0.2pt] (207.5,0) -- (207.5,241);

  % Auteur horizontal en haut du dos
  \node[anchor=north, text=encre] at (182.75, 227)
    {\fontsize{11}{13}\selectfont\textls[120]{\textsc{éric mugnier}}};

  % Titre vertical (de bas en haut)
  \node[rotate=90, anchor=center, text=encre] at (182.75, 121)
    {\fontsize{20}{24}\selectfont\textls[80]{\textsc{tête de veau ravigote}}};

  % ── Logo CT (dos, bas, discret) ───────────────────────────────────────────
  \node[anchor=south, opacity=1] at (182.75, 10)
    {\includegraphics[width=6mm]{images/ct_watermark.png}};

  % ═══════════════════════════════════════════════════════════════════════════
  % ══ 4ÈME DE COUVERTURE (3 – 158 mm) — centre : 80,5 mm ═════════════════
  % ═══════════════════════════════════════════════════════════════════════════

  % ── Avertissement + épigraphe ─────────────────────────────────────────────
  \node[
    anchor=north west,
    inner sep=0pt,
    text=encre
  ] at (17, 218)
  {%
    \begin{minipage}[t]{124mm}
      \setlength{\parindent}{0pt}%
      \setlength{\parskip}{\baselineskip}%
      % — corps —
      {\large\input{_avertissement-body.tex}\par}%
    \end{minipage}%
  };

  % ── Épigraphe (bas de la 4e de couverture) ────────────────────────────────
  \node[
    anchor=south west,
    inner sep=0pt,
    text=encre
  ] at (17, 40)
  {%
    \begin{minipage}[t]{124mm}
      \setlength{\parindent}{0pt}%
      {\large\input{_epigraphe-huysmans.tex}}%
    \end{minipage}%
  };

\end{tikzpicture}

\end{document}
